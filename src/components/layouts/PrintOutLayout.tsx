// src/layouts/PrintOutLayout.tsx
import React from "react";
import { Outlet } from "react-router-dom";

const PrintOutLayout: React.FC = () => {
  return <Outlet />;
};

export default PrintOutLayout;
